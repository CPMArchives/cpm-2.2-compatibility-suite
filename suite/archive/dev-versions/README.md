# CP/M compatibility utility development archive

This archive contains recoverable source and assembled COM revisions only; disk images are excluded. Historical binaries previously recovered from runtime images are now preserved directly here; rebuilding the manifest does not depend on those obsolete images. A source file is included only when its embedded version matches the archived revision. See MANIFEST.tsv for provenance and SHA-256 checksums.
