#!/usr/bin/expect -f
# SPDX-License-Identifier: GPL-2.0-or-later
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set cpu [lindex $argv 2]
if {$cpu eq ""} { set cpu "-z" }
set timeout 30
log_file -noappend $logfile
source [file join [file dirname [info script]] cpmsim-path.exp]
spawn $cpmsim $cpu -d $diskdir
expect "A>"
foreach cmd {"FILETEST /LIST" "FILETEST /0171" "FILETEST /CASE:BDOS-FILE-001-P0171" "FILETEST /GROUP:OPEN" "FILETEST /FN:15" "FILETEST /SAFE"} {
 send -- "$cmd\r"
 expect "A>"
}
send "\034"
expect eof
