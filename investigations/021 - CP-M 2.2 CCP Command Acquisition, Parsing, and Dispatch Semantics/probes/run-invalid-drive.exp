#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 20
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
send "Q:\r"
expect "Bdos Err On Q: Select"
send "x"
expect "A>"
send "\034"
expect eof
