#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 30
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
send -- "CROSS45\r"
expect "CROSS45 COMPLETE"
expect "A>"
send "\034"
expect eof
