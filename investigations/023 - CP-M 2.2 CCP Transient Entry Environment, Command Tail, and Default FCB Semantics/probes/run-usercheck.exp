#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 30
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
send "USER 1\r"
expect "A>"
send "B:STATE22 CHECK\r"
expect "A>"
send "B:ENTRY23 CHECK\r"
expect "A>"
send "\034"
expect eof
