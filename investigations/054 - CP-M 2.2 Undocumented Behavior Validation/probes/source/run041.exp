#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 120
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
foreach p {VECTOR41 ZERO41 BDOS41 MOD41 BIOS41} {
  send "$p\r"
  expect "A>"
}
send "\034"
expect eof
