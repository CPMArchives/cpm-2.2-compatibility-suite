#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 30
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
send "DIRECT41\r"
expect "invalid-SELDSK-HL"
expect -re { [0-9A-F]{4} [0-9A-F]{4}}
send "\034"
expect eof
