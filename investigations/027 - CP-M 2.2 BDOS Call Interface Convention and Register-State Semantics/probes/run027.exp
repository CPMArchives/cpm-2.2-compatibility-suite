#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 60
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
foreach cmd {CALL27 FUNC27 REG27 STACK27 PARAM27} {
 send -- "$cmd\r"
 expect "A>"
}
send "\034"
expect eof

