#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 60
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
foreach cmd {STATE26 A:DPB26 A:RESET26 A:PROTEC26} {
 send -- "$cmd\r"
 expect -re {[AB]>}
}
send "\034"
expect eof

