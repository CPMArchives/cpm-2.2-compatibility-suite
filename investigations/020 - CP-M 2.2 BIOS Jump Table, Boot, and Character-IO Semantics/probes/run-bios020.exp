#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 30
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
send "BIOS020\r"
expect "A>"
send "WBOOT20\r"
expect "A>"
send "PZERO20\r"
expect "A>"
send "\034"
expect eof
