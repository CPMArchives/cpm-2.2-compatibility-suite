Investigation 032 deterministic CCP command tests

DISK32.txt, ERA32.txt, REN32.txt, SAVE32.txt, TYPE32.txt, USER32.txt, and
PARSE32.txt contain each test's purpose, procedure, observation, and conclusion.
run-main032.exp performs the accepted noninteractive command corpus on restored
controlled images. run-error032.exp isolates invalid-drive fatal presentation.

CONFLICT.ASM builds the marker COM installed under all six resident names.
The marker text never appeared for unprefixed resident invocations.
make-fixtures.sh rebuilds the images with controlled users, attributes, text,
binary/control data, rename/delete targets, and allocation-full D:.

The initial run against a full inherited directory and the early binary fixture
run are rejected development runs and were overwritten, not preserved as evidence.
