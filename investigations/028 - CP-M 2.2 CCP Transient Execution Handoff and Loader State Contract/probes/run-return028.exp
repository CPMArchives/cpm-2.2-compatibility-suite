#!/usr/bin/expect -f
set diskdir [lindex $argv 0]
set logfile [lindex $argv 1]
set timeout 60
log_file -noappend $logfile
spawn cpmsim -z -d $diskdir
expect "A>"
send -- "B:RETURN28\r"
expect -re {[AB]>}
send -- "B:CHECK28\r"
expect -re {[AB]>}
send "\034"
expect eof
